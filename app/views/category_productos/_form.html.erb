<%= form_with(model: category_producto, local: true) do |form| %>
  <% if category_producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_producto.errors.count, "error") %> prohibited this category_producto from being saved:</h2>

      <ul>
      <% category_producto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :producto_id %>
    <%= form.text_field :producto_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
